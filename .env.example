# Environment variables for the Tandem dashboard app.
#
# Canonical template: apps/dashboard/.env.example
# Local dev:          copy to apps/dashboard/.env.local and fill in values
# Replit:             set all variables via Replit Secrets (Settings → Secrets)
#
# This root file is a pointer only — do not add variable definitions here.
